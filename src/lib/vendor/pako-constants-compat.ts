import * as ConstantsModule from '../../../node_modules/pako/lib/zlib/constants.js';

const constants = (ConstantsModule as { default?: unknown }).default || ConstantsModule;

export default constants;
