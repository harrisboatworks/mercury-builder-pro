

# Create Agent API Instructions Markdown File

Add a markdown file to the project root containing the full instruction document for configuring an external AI agent (Manus/OpenClaw) to use the agent-quote-api endpoint. This is the same content previously generated in chat, saved as a downloadable/reference file.

## File

`AGENT_API_INSTRUCTIONS.md` — contains endpoint URL, auth header, all 5 actions with request/response examples, pricing logic explanation, and example workflows.

