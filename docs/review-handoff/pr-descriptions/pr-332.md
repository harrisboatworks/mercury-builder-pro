# Local description artifact for #332 — not posted

**Do not post this comment or replace the GitHub body from this file unless an owner asks.** Release status is unchanged: **draft**. Coordinated secret + paired deploy still required.

## Current head

`38e49fb0cb2be2427d8171b602f9541f92db7aee` on `cursor/twilio-webhook-signature-1b4b`.

GitHub body still cites `c5b1e39e`. That string is stale.

Base compare / merge-base vs `main`: `431c63471b84230208c03837226f40ec82f0058a`. Current `origin/main` tip: `609c63f026f355200a64a9c88093aac4bdbf3942`. No GitHub assignee.

Range vs merge-base: 14 files, +1035 / −107.

## Actual changes on this head

- Inbound `X-Twilio-Signature` validation on the configured canonical HTTPS URL plus the one allowlisted UUID query. Request/proxy host and path never participate.
- Repeated form keys preserved for Twilio HMAC; fail-closed without `TWILIO_AUTH_TOKEN` or `TWILIO_WEBHOOK_URL`.
- Service-role client created only after URL, body, signature, SID, and status validation.
- Status/SID persistence uses one conditional PostgREST update (monotonic under out-of-order callbacks).
- Deploy preflight: `runDeploy` lists Edge secret **names** for the resolved project and fails closed if `TWILIO_WEBHOOK_URL` is missing. No values or digests printed.
- Source file `supabase/migrations/20260830230000_add_twilio_sms_status_tracking.sql` is **already on `main` / production** as ledger `20260910005522` / `add_twilio_sms_status_tracking`. **Never reapply.**

Intended canonical URL (already documented; not written by this review): `https://eutsoqdpjurknjsshxes.supabase.co/functions/v1/notification-webhook`. Owner metadata: token name present, URL name absent. This reviewer did not read production secret values.

## Observed hosted CI (GitHub, not this reviewer)

On head `38e49fb0c` (Actions around 2026-09-11T22:06Z): `test`, Frontend typecheck, Edge typecheck, integrity **success**. `production-quote`, `production-parity`, and Financing integration **skipped**. Example test job: https://github.com/harrisboatworks/mercury-builder-pro/actions/runs/34652464879

Skipped production probes are not live SMS acceptance.

## This reviewer’s synthetic claims (combo replay, not GitHub)

On the local four-PR combo that includes this head, no live Twilio:

- Webhook signature + SMS status tests included in 176 focused passed (Twilio published HMAC vector, repeated-key vector, fail-closed without URL/token).

Those numbers are local combo results.

## Remaining release gates (unchanged)

- Set `TWILIO_WEBHOOK_URL` to the exact normalized public callback URL.
- Deploy `send-sms` and `notification-webhook` together after review.
- Do not reapply the tracking migration.
- `send-notification` tracking remains a separate lane.

## Integration note

Locally combinable with #528, #371, and #542 onto `609c63f02`. Twilio slugs do not overlap cron-auth or pair slugs. Manual combo test union kept this PR’s preflight describe next to #528’s cron-auth describes. No unowned integration defect found. See `docs/review-handoff/INTEGRATION-MATRIX.md` on `cursor/four-pr-integration-diagnostic-3e2c`.
