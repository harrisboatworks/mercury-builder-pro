# Local description artifact for #371 — not posted

**Do not post this comment or replace the GitHub body from this file unless an owner asks.** Release status is unchanged: **draft**. No isolated staging target is approved.

## Current head

`896d74198a961d4d2c4710625929c7e50f67b8fe` on `cursor/deposit-deal-packet-20260823`.

GitHub body still cites `7a86e715`. That string is stale.

Base compare / merge-base vs `main`: `431c63471b84230208c03837226f40ec82f0058a`. Current `origin/main` tip: `609c63f026f355200a64a9c88093aac4bdbf3942`. No GitHub assignee. Owner must create a **new** data-less isolated staging project. Do not reuse production `eutsoqdpjurknjsshxes`.

Range vs merge-base: 102 files, +19050 / −1343.

## Actual changes on this head

- Motor-deposit deal packet: authority derivation, checkout/webhook reconciliation, quote-document delivery, per-audience email outbox/retry, admin deal-packet view.
- Public deposit-tier bypass closed: amount from authoritative `motor_models.horsepower` (≤25 HP $200; >25–115 $500; >115 $1000). $100 express reservation remains limited to the verified Mercury 9.9 identity.
- New migration `supabase/migrations/20260823120000_deposit_deal_packet.sql` (not on `main`, not applied here). Later unique-binding / claim migrations already exist on `main`.
- Official PostgreSQL 17 locator shared with #528 (`local-postgres17.mjs` identical).
- Hosted bootstrap refuses the production project ref and requires an isolated nonce.

## Observed hosted CI (GitHub, not this reviewer)

On head `896d74198` (Actions around 2026-09-11T22:04Z): `test`, Frontend typecheck, Edge typecheck, integrity, drift, outdoor-only-policy **success**. `production-quote`, `production-parity`, and Financing integration **skipped**. Example test job: https://github.com/harrisboatworks/mercury-builder-pro/actions/runs/34652332503

Skipped production probes are not hosted staging acceptance.

## This reviewer’s synthetic claims (combo replay, not GitHub)

On the local four-PR combo that includes this head, official PG 17.11, unix socket only, synthetic fixtures:

- `npm run test:deposit-acceptance`: 18 files / 193 passed.
- `npm run test:deposit-acceptance:pg`: `[LOCAL-PG-SYNTHETIC]` 114/114; `hosted_isolated_staging=outstanding`.

Those numbers are local combo results. They do not prove a hosted isolated staging project.

## Remaining release gates (unchanged)

- Approve and bind a **new** isolated staging project. Do not repurpose production or unrelated projects.
- Keep draft until migration → Edge → frontend order is owner-reviewed and synthetic hosted acceptance runs on that project.
- No real Stripe charges, customer email, or SMS.
- Deno acceptance remains a separate pin/verify item on the owner path.

## Integration note

Locally combinable with #528, #332, and #542 onto `609c63f02`. Deposit migration versions before Twilio tracking and cron-auth rewrite. No unowned integration defect found. See `docs/review-handoff/INTEGRATION-MATRIX.md` on `cursor/four-pr-integration-diagnostic-3e2c`.
