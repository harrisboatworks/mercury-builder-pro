# Local description artifact for #542 — not posted

**Do not post this comment or replace the GitHub body from this file unless an owner asks.** Release status is unchanged: **draft**. Guards stay `UNVERIFIED`. Do not mark `ATTESTED` from synthetics.

## Current head

`9f7544ef063244d20b57c59c5c82986f89c128ab` on `cursor/pair-attestation-probes-3e2c`.

This matches the GitHub head when read on 2026-09-12.

GitHub `base.sha` on the PR object was `397654fe566cb207d75be5b619371bc0c8e9531e`. Current `origin/main` tip: `609c63f026f355200a64a9c88093aac4bdbf3942`. Merge-base of this head vs current `main`: `431c63471b84230208c03837226f40ec82f0058a`. No GitHub assignee. Codex owns live probes in a credential-owning environment.

Range vs merge-base: 7 files, +1370 / −3.

## Actual changes on this head

- Offline pair-attestation harness (`scripts/lib/public-function-pair-attestation.mjs` + CLI + Vitest).
- Final cleanup uses **only the last** `close()` result.
- Missing or throwing `hasLiveResources()` is `leftover=true` (fail closed).
- Function presence is not AbortSignal proof (`fetchSupportsAbort` / `webrtcSupportsAbort` / `adaptersHonorAbortContract` return false).
- CLI without an injected fetch adapter exits 1 and does not open OpenAI.
- Docs/runbook only for probe procedure. Committed pair attestations remain `UNVERIFIED`.

## Observed hosted CI (GitHub, not this reviewer)

On head `9f7544ef0` (Actions around 2026-09-12T00:09Z): `test`, Frontend typecheck, Edge typecheck, integrity, drift, outdoor-only-policy **success**. `production-quote`, `production-parity`, and Financing integration **skipped**. Example test job: https://github.com/harrisboatworks/mercury-builder-pro/actions/runs/34660655061

Older failed `test` runs on `b9c795b35` / `f274f1d70` are superseded by this head.

## This reviewer’s synthetic claims (combo replay, not GitHub)

On the local four-PR combo that includes this head, no live OpenAI:

- `src/test/publicFunctionPairAttestation.test.ts` included in 176 focused passed (last-close, absent readback, function-presence-is-not-proof).
- `npm run check:pair-attestation-probe` exits 1 with “No injected fetch adapter…” on both the combo and this head alone.

Those numbers are local results. They are not production attestation.

## Remaining release gates (unchanged)

- Codex live probes only in a credential-owning environment.
- Do not mark site-chat or openai-realtime `ATTESTED` from this PR.
- Pair UNVERIFIED holds remain on deploy.

## Integration note

Locally combinable with #528, #371, and #332 onto `609c63f02`. Pair slugs do not overlap cron-auth or Twilio slugs. No unowned integration defect found. See `docs/review-handoff/INTEGRATION-MATRIX.md` on `cursor/four-pr-integration-diagnostic-3e2c`.
