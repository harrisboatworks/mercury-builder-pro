# Local description artifact for #528 — not posted

**Do not post this comment or replace the GitHub body from this file unless an owner asks.** Release status is unchanged: open, ready for code review, **not** merge-ready. Manifest stays `unresolved`.

## Current head

`cebec56ad573fedd6f1dc00efaf35add77602f97` on `codex/cursor-cron-auth-20260910`.

GitHub body still cites `bc9652d65`. That string is stale.

Base compare / merge-base vs `main`: `431c63471b84230208c03837226f40ec82f0058a`. Current `origin/main` tip: `609c63f026f355200a64a9c88093aac4bdbf3942` (three later blog commits: #543, #545, #546). No GitHub assignee. Owner/Codex lane for Vault, provision, parity, and authorize/apply.

Range vs merge-base: 19 files, +1897 / −49.

## Actual changes on this head

- Authenticated handlers for `check-expiring-promotions` and `sync-lightspeed-inventory` (`x-internal-secret == first nonempty of EDGE_INTERNAL_SECRET || CRON_SECRET`). Gateway `verify_jwt=false` unchanged.
- Authored caller migration `supabase/migrations/20260910140000_rewrite_promo_lightspeed_cron_internal_secret.sql`. **Not applied.**
- Explicit deploy hold: `scripts/lib/cron-auth-release-prerequisites.json` remains `status=unresolved`, `path=null`, `version=null`.
- Resolved-manifest entries must match `CRON_AUTH_CALLER_MIGRATION_PATH` and `CRON_AUTH_CALLER_MIGRATION_VERSION` (P1 fix already on this branch).
- Official PostgreSQL 17 locator for disposable caller-migration fixtures (`scripts/lib/local-postgres17.mjs`).

## Observed hosted CI (GitHub, not this reviewer)

On head `cebec56ad` (Actions around 2026-09-11T22:04Z): `test`, Frontend typecheck, Edge typecheck, integrity, drift, outdoor-only-policy **success**. `production-quote`, `production-parity`, and Financing integration **skipped**. Example test job: https://github.com/harrisboatworks/mercury-builder-pro/actions/runs/34652330952

Skipped production probes are not live acceptance.

## This reviewer’s synthetic claims (combo replay, not GitHub)

On the local four-PR combo that includes this head, with official PG 17.11 and no live Vault/HTTP:

- `cron-auth-gate` + deploy mapping / resolved-identity tests: included in 176 focused passed.
- `CRON_AUTH_REQUIRE_PG=1` caller-migration: 28/28 on disposable fake cron/Vault/net.

Those numbers are local combo results. They do not replace hosted CI on `cebec56ad` alone.

## Remaining release gates (unchanged)

- Accept `EDGE_INTERNAL_SECRET` as the proposed Vault name (or revise it). Provisioning, value parity, execution-role read, protected rollback snapshot, and applied ledger/caller outcomes remain prerequisites.
- Do not apply `20260910140000` without separate authorization.
- Do not deploy the two protected slugs while the manifest is unresolved.
- Do not reuse the ElevenLabs secret.
- Uppercase Vault names are a proposed contract, not an established provisioned fact.

## Integration note

Locally combinable with #371, #332, and #542 onto `609c63f02`. Cron slugs do not overlap Twilio or pair slugs. No unowned integration defect found. See `docs/review-handoff/INTEGRATION-MATRIX.md` on `cursor/four-pr-integration-diagnostic-3e2c`.
